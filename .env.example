# 复制成 .env，填上后端的公网 https 地址（Tailscale Funnel 那个）。
# .env 不进仓库；部署时用的是同名的 GitHub secret API_BASE。
#
#   cp .env.example .env
#   ./make-config.sh        # 由 .env 生成 config.js，同样不进仓库
#
API_BASE=https://your-backend.example.ts.net:8443
